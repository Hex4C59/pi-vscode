# pi VS Code 领域词汇表

[English](CONTEXT.md) | 中文

- 翻译状态：Machine Draft
- 权威原文：[CONTEXT.md](CONTEXT.md)
- 原文版本：Uncommitted baseline
- 最近同步：2026-09-22

本词汇表只定义产品术语，不定义实现或交付状态。行为与验收归 [PRD](docs/product-requirements.zh.md) 管理。

## 术语

**扩展加载策略（Extension loading policy）**：决定哪些可执行 pi 扩展可以加载：仅内置或显式信任的第三方扩展。
_避免混称_：工具审批、沙箱模式。

**工具审批策略（Tool approval policy）**：对覆盖范围内工具调用允许、询问或拒绝的规则，不描述扩展代码能够执行的所有行为。
_避免混称_：扩展信任、文件系统隔离。

**项目资源同意（Project-resource consent）**：用户对加载受保护项目本地 pi 资源的选择，不同于工作区信任和工具授权。
_避免混称_：项目全部权限。

**活跃运行会话（Live session）**：在其项目中当前运行的 Agent 对话实例，不同于已保存历史。
_避免混称_：描述临时授权生命周期时使用已保存记录。

**已保存会话（Saved session）**：为之后恢复而保留的对话，不是项目文件快照或运行中的进程。
_避免混称_：代码检查点、运行进程接管。

**会话顺序交接（Sequential session handoff）**：离开原入口后，在另一个入口继续已保存对话。
_避免混称_：并发控制、强制排他所有权。

**交互取消（Interaction cancellation）**：拒绝或关闭一次扩展交互，不等于停止整个任务。
_避免混称_：仅取消对话框时称为 Stop。

**停止（Stop）**：请求停止活跃任务与排队续接，不是撤销已经完成的副作用。
_避免混称_：回滚、终止全部进程保证。

**附件快照（Attachment snapshot）**：某一时点作为显式消息上下文提供的文本，不同于之后的编辑器或磁盘内容。
_避免混称_：将历史附件文本称为当前文件。

**审阅快照（Review snapshot）**：用于检查一次已应用修改的前后内容。
_避免混称_：待应用补丁、当前工作区差异、回滚检查点。

**工具报告的目标（Tool-reported target）**：工具操作指明的文件，单凭它不能证明完整修改归属。
_避免混称_：全部 Agent 修改。

**观察到的工作区变化（Observed workspace change）**：任务期间观察到的文件变化，可能还包含用户、shell 或并发活动。
_避免混称_：无归属证据时称为纯 Agent 差异。

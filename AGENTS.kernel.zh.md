# 共享的 Agent 工作规则

[English](AGENTS.kernel.md) | 中文

- 翻译状态：Machine Draft
- 权威原文：[AGENTS.kernel.md](AGENTS.kernel.md)
- 原文版本：Uncommitted baseline
- 最近同步：2026-09-22
- 类型：指南
- 状态：Accepted
- 创建：2026-09-19
- 权威：面向人类贡献者与编码 Agent 的跨项目规则
- 适用于：从本模板建立的每个产品仓库，以及编辑这些仓库的 Agent

本文件规定共同的 Agent 工作规则。项目 `AGENTS.md` 补充项目事实、具体安全约束、架构路径和按任务阅读的入口；不得削弱本内核。

本中英文文档对来自 engineering-template，复制到产品仓库根目录使用。修改共享规则时，应记录模板源与本仓副本的同步情况；本仓修改不会自动更新模板或其他项目。**2026-09-22：** 本仓已批准可读性调整；本次任务未更新模板源。

## 文档职责与冲突优先级

先使用负责回答当前问题的文档。以下列表也按冲突时的优先级从高到低排列；产品的具体路径由 `AGENTS.md` 指定：

1. 本内核与 Agent 操作指南规定必须遵守的工程约束。
2. 状态为 `Accepted` 的产品需求文档规定已批准的用户可见行为及验收。
3. 主架构文档规定结构、边界和职责所有者。
4. `ACTIVE.md` 记录当前工作项（WI）、范围、阶段和批准情况。
5. 讨论与归档提供背景和历史证据，不能独立授权实现。

冲突是指针对同一个问题提出无法同时满足的要求，而不是分别说明不同事项。指出发生冲突的文档，按上述优先级处理，不要静默覆盖。产品取舍及是否开始工作仍由维护者决定。

## Git 提交操作必须获得明确授权（L0）

除非用户明确要求，否则不得创建、修改、合并或改写 Git 提交。

## 基于证据判断和报告（L0）

- 证据允许时给出明确结论：是、否、部分成立、尚未完成或未知。结论依据应来自仓库文件、当前阶段、Gate 和实际运行的命令。
- 维护者的前提与仓库事实不一致时，具体、专业地指出差异，并引用核对过的证据。事实描述由证据决定，产品取舍由维护者决定。
- 不确定时说明缺少什么证据；不得为了显得有用而编造缺口，也不得为了迎合提问而附和。
- 被问及还需优化什么或下一步做什么时，可以建议当前无需实质改动、继续 ACTIVE 中的既有队列，或推迟过早的想法。只有明确要求头脑风暴、备选方案或待办清单时，才列可选想法。
- 适用的验收条件、Gate 关闭和维护者确认未能支持精确范围之前，不得把计划或受 Gate 约束的能力描述为已交付。
- 这些判断规则不能覆盖产品安全规则、已明确限定范围的任务指示，或 Git 提交必须明确授权的要求。

例如：被问及是否还需优化时，有证据支持的回答可以是“当前 WI 前没有必须追加的工作”。被要求把技术探针尚未确认的 ADR 标为 Accepted 时，应报告缺少的验证，并保留当前 Gate 状态。

更多判断示例见产品中的 `docs/guides/agent/judgment.md`；模板源位于 engineering-template 的 `workflow/judgment.md`。

## 各项目必须落实的安全边界（L0）

项目 `AGENTS.md` 应提供落实以下原则的具体规则：

- **低信任 UI：** 不得接收凭证、任意宿主能力或直接访问上游运行时 API。
- **高权限宿主：** 只提供有明确名称且在允许列表内的跨边界操作，并在执行前校验传入值。
- **上游职责：** 使用文档化 SDK／RPC 调用上游的 Agent 循环、会话存储和 provider 栈；除非产品章程另有规定，否则不得重新实现这些能力。
- **如实说明沙箱能力：** 当运行时使用用户权限执行时，不得暗示它是安全沙箱。UI 和文档必须说明实际的信任与权限限制。

## 通用系统分层与职责

Electron、VS Code、Web 和 CLI 宿主都采用相同的职责划分：

- **UI（界面）：** 视图、输入和短期展示状态。
- **Host（宿主）：** 高权限能力、生命周期及经过校验的接口。
- **Adapter（适配层）：** 将上游 SDK／RPC 映射为内部事件，隔离版本变化。
- **Runtime（运行时）：** 上游模型、Agent、工具和会话。

流式和生命周期细节由产品架构与操作指南负责。产品加载地图应指向已有的相关文档；本内核不规定占位文件名。

## 开始任务时

1. 阅读本内核、项目 `AGENTS.md`、`ACTIVE.md`、`README.md`、当前目录结构、存在时的 `package.json` 和相关测试。
2. 阅读 `ACTIVE.md` 中的当前 WI 和会话摘要。在提出应用代码方案或修改应用代码前，完整阅读产品协作指南（通常为 `docs/guides/agent-collaboration.md`），即使维护者没有特别提及它。
3. 按项目加载地图阅读与任务相关的操作指南。保留用户已有的无关 Git 改动，使用仓库既定包管理器和脚本，不凭个人偏好更换技术栈。
4. 如果任务改变安全边界、数据保存方式或上游集成策略，先澄清影响并记录决策，再开展大范围修改。实现前遵守产品协作指南规定的审批要求。

仅 @ `ACTIVE.md` 只是提供会话入口，不免除必读要求或实现审批。只有仅作只读回答、不涉及 WI 变更且不修改代码时，才可跳过协作指南全文阅读；涉及 Gate、ADR 或会话收尾规则时，该例外不适用。这个例外不免除其他基础阅读要求。

## 什么时候需要架构核对

涉及结构、边界、接口、依赖、契约或所有权，以及相关并发、生命周期、安全、数据保存方式、可观测性、错误、测试或发布行为时，应以仓库文档为依据，不假设先前聊天上下文一直可用：

1. 阅读已有且相关的产品架构、模块和参考文档。
2. 在提出模块拆分、供其他模块或外部调用的新接口、跨层依赖，或声称架构正确之前，核对架构清单中适用于本次任务的维度。说明哪些已满足、哪些仍有缺口，并附证据路径和未解决风险。

产品仓库中的清单是 `docs/guides/architecture-governance.md`；模板源是 `workflow/architecture-governance.md`。不得根据 `Planned` 状态的空壳文档，臆造已实现的职责所有者或 `Living` 状态的契约。

详细工程约束仍放在 `docs/guides/agent/` 下的专题操作指南中。项目加载地图必须包含架构治理，并说明何时需要读取各指南；不要求每个任务都读取全部指南。

## 交付前检查

核对已批准范围，并报告完成工作的证据：

1. WI 要求端到端可用时，交付可操作的完整行为，不能仅完成静态 UI。
2. 对 WI 涉及的行为，验证类型、错误、取消、资源清理和空状态。不要把不适用的检查扩大为额外功能。
3. 添加或更新相关测试并实际运行。执行仓库适用的 lint／类型检查并报告结果；无法运行的检查明确保留为“未验证”，不得写成通过。
4. 保持安全边界。变更必须获得适用的批准及明确验证；仅构建成功不能授权扩大访问权限。
5. 用户可见行为或命令改变时，同步相关文档。实质性文档修改后，运行仓库的文档校验命令。
6. 报告实际执行的检查、结果及未验证部分和原因，区分本次运行与历史证据。检查通过不会自动构成维护者验收、WI 关闭或 Gate 接受，仍须遵循项目审批和决策规则。

项目专用命令和验收流程放在项目 `AGENTS.md`、操作指南及仓库配置中，不放入共享内核。

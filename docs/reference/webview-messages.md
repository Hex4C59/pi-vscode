# Webview messages (workspace, chat, model settings and controlled execution)

English | [中文](webview-messages.zh.md)

- Type: Reference
- Status: Outline
- Created: 2026-09-19
- Contract ID: `contract-webview-messages`
- Owner: extension host (`src/extension/`); UI sender in `src/webview/`
- Gate: `gate-webview-trust` (Open)

> Connectivity, workspace choice, a host-owned pi RPC subprocess (WI-007), plain-text chat (WI-004), model/thinking settings (WI-008/WI-009) and controlled execution (WI-010). No secrets, pi SDK in the webview, filesystem access or generic host operations are exposed. Trust and session-streaming gates remain open.

## Envelope and allowlist

**Migration note (T014-01 Build):** the following v1 shapes preserve the earlier controlled-chat contract for historical comparison. Current source rejects v1 and uses v2 plus `viewId`; `sendChat` now refers to a host draft revision. The T014-01 section below owns the replacement fields. The independent approval-gate envelope remains v1.

Plain objects only, numeric `version: 1`, exactly the listed own fields. The host validates `unknown` before acting. Unknown types/versions, extra fields, malformed values and stale generations cannot execute actions.

- Webview → host `ping`: `{ version: 1, type: "ping" }`; replies with `{ version: 1, type: "pong" }`.
- Webview → host `getWorkspaceState`: `{ version: 1, type: "getWorkspaceState" }`; synchronizes the current host projection.
- Webview → host `openFolder` / `manageTrust`: `{ version: 1, type: "openFolder" | "manageTrust", generation: number }`.
- Webview → host `chooseResources`: `{ version: 1, type: "chooseResources", generation: number, choice: "allow" | "decline" }`.
- Webview → host `sendChat` (WI-004): `{ version: 1, type: "sendChat", generation: number, text: string }`. `text` must be non-empty after trim and at most `8000` characters. Allowed only when `runtime === "ready"`, generation matches, workspace actions are not `busy`, `modelBusy` is false, and no chat turn is in flight.
- Webview → host `setThinkingLevel` (WI-008): `{ version: 1, type: "setThinkingLevel", generation: number, level: string }`. `level` must match the host-projected allowlist (`thinkingLevels`) and a bounded token shape. Allowed only when `runtime === "ready"`, generation matches, workspace actions are not `busy`, and `modelBusy` is false. `chatBusy` does not prohibit selection: while streaming, record next-turn intent without mutating the running model/thinking settings.
- Webview → host `setChatModel` (WI-008): `{ version: 1, type: "setChatModel", generation: number, provider: string, modelId: string }`. `provider` and `modelId` must match a bounded token shape and an entry in the host-projected `availableModels` list. Same eligibility as `setThinkingLevel`.

Rejected malformed messages receive no response. Stale or ineligible valid actions only resynchronize state. Ping has no business side effects. No action accepts a path, command, trust boolean or runtime request from the webview.

## Host state projection

The host sends `version: 1, type: "workspaceState"`. [`WorkspaceStateMessage`](../../src/extension/bridge/webviewMessages.ts) owns the complete field/type declaration; this table defines semantic groups. The UI renders this projection instead of inventing authoritative state.

| Fields | Meaning and limits |
|--------|--------------------|
| `generation`, `status`, `folder` | Nonnegative safe-integer generation; status is `no-folder`, `multi-root`, `remote`, `non-file`, `untrusted` or `eligible`. Folder is `{ name, path }` for one folder or null. |
| `choice`, `busy`, `error` | Choice is null/`allow`/`decline`; busy tracks unfinished workspace actions; error is null or a fixed bounded recovery message, without raw exceptions. |
| `runtime`, `runtimeDetail` | Phase is `not-started`/`starting`/`ready`/`stopping`/`error`; failure detail is a bounded host diagnostic or null, without raw exception dumps. |
| `messages`, `chatBusy`, `chatError` | Ordered host-owned `{ role: "user" \| "assistant", text, id? }` entries, with optional correlation id. Busy starts at submission and lasts until settlement/failure. Chat errors are bounded, without raw stderr or credentials. |
| `chatModel`, `thinkingLevel`, `thinkingLevels`, `availableModels` | Applied model label/level can be null; capability lists are bounded, with at most 64 `{ provider, modelId, label }` model entries. |
| `pendingModel`, `pendingThinkingLevel` | Null or a pending catalog entry/valid level; host-memory intent is distinct from applied values. |
| `modelBusy`, `modelError` | Catalog loading, serialized application and recovery block sends and further selections; failure text is bounded or null. |
| `activities`, `approvals`, `grants`, `execution`, `controlledExecution` | Execution projections defined below; `controlledExecution: true` identifies the profile, not readiness or gate acceptance. |

## Controlled execution contract

Execution actions use the envelope rules above and these inbound shapes:

- `stopChat`: `{ version: 1, type: "stopChat", generation: number }`; only an active chat not already stopping is acted on.
- `decideApproval`: `{ version: 1, type: "decideApproval", generation: number, id: string, decision: "once" | "session" | "deny" }`.
- `revokeGrant`: `{ version: 1, type: "revokeGrant", generation: number, id: string }`.

IDs must be nonempty and at most 100 characters. Existing exact-own-field validation, active-view, generation and workspace-busy checks apply. IDs select host-owned pending requests/grants, never a webview-supplied command or path. Unknown/expired decisions cannot revive a removed request. `session` creates a grant only when a non-null reliable scope exists; absent scope provides no reusable authorization and UI must offer Allow once only.

Execution projection fields:

- `activities`: at most 64 host-owned `ActivityItem` entries: `{ id, kind: "thinking" | "tool", messageId, contentIndex?, toolCallId?, tool?, text, input?, status, truncated }`. Status is `thinking`, `preparing`, `executing`, `complete`, `failed` or `interrupted`. Thinking correlates by message/content index; tools by tool-call ID. Text and displayed input are bounded to 16,384 characters per item (bounded aggregate by item count). Text or input overflow sets `truncated`, retained across tool updates. The 64-entry cap reserves a stable `activity-overflow` notice: additional activity is omitted from display, not execution/approval checks. The UI renders truncation notices and incremental stable-ID details, preserving expansion/focus/scroll. `tool_execution_start` projects `preparing`, not side-effect confirmation. `partialResult` replaces cumulative output. `message_end` reconciles final text/thinking; no synthetic thinking when absent.
- `approvals`: at most 8 `{ id, toolCallId, tool, input, scope: string | null, expiresAt }` cards. `input` is the full JSON argument snapshot, at most 32,768 characters; oversized or credential-like inputs are denied before projection. Default host/gate timeout is 120 seconds. Denial, expiry, cancellation and late replies never confer approval.
- `grants`: at most 64 `{ id, scope }` entries. Existing regular-file scope encodes `[tool, canonicalPath]`; shell scope encodes `[tool, canonicalCwd, completeInput]`. Scope equality is exact; no implicit directory/command-prefix grants. Nonexistent/unresolvable targets have null scope. Inspect/revoke affects subsequent calls, not past effects.
- `execution`: `idle`, `waiting`, `thinking`, `awaiting-approval`, `executing`, `replying`, `stopping` or `failed`; a presentation state, not evidence of sandboxing. `controlledExecution` describes the selected profile, not proof of runtime readiness or gate closure.

`toolApproval.ts` auto-allows only canonical in-workspace regular-file `read`. Search/list still ask, as do edits/writes, shell and external paths; unknown tools fail closed. Windows ambiguous paths are not auto-authorized; realpath checks cover symlink/junction escapes but cannot prevent check/use races. Same-live-session grants survive view recreation, ordinary settlement and successful Stop; workspace/eligibility change, runtime replacement/disconnect and provider disposal cancel requests and clear grants.

Host cancels pending approvals before `abortTask`; adapter cancels outstanding dialog waits, sends `clear_queue` then `abort` with bounded waits. Success requires settlement; failure or lack of settlement shuts down runtime and reports failure. Host keeps `stopping` through cancellation and delays pending settings application until it completes. Runtime loss marks unfinished activity interrupted; no automatic task replay. Stop does not undo side effects or guarantee cancellation of every descendant process.

The adapter accepts only the bundled gate's product envelope `protocol: "pi-vscode-approval", version: 1`, with runtime/cwd matching the current subprocess and `kind: "hello"` via notify before readiness. Calls carry `request`, `toolCallId`, `tool`, `input`; the bundled asynchronous `tool_call` handler waits via public `ctx.ui.confirm` and checks the full argument snapshot again after confirmation. RPC dialog IDs correlate `extension_ui_response`; title text is not authority. This is not a generic `approve_tool_call` RPC. The controlled tool allowlist is `read,write,edit,bash,powershell,grep,find,ls` with `--no-extensions -e <bundled gate>`; third-party extension discovery is disabled for either project-resource choice. Missing bundle blocks startup; missing hello/readiness terminates it, with no usable no-tools fallback promised. The offline load-failure fixture separately uses `--no-tools`.

Forward only bounded presentation fields, never raw RPC objects/stderr, signatures or credential stores. Pattern filtering is best-effort and cannot guarantee arbitrary thinking/tool output is secret-free. [Architecture §7](../architecture/vscode-extension-architecture.md) owns process permissions and environment controls; verification records are linked under Evidence boundaries below.

## Deferred model/thinking settings (WI-008 / WI-009)

- Idle valid selections apply immediately. During `chatBusy`, selection only overwrites its own pending field (latest model and latest thinking independently win); no settings RPC runs during the active session-level run. The combined chip always shows applied values; a separate status shows `Next turn (pending)` / `Applying next turn`, including when the popover is closed.
- On the current runtime session's `agent_settled`, `applyPendingSettings` sets `modelBusy` before releasing the UI to send, applies `set_model` first, accepts its refreshed state/catalog/capabilities, and revalidates the pending thinking level before `set_thinking_level`. Unsupported or unapplied thinking produces a bounded error, not a silent clamp or fallback. The model may already have changed; this is not an atomic rollback transaction.
- Failure triggers one projection readback, not mutation retry. If readback fails, applied model/level and available levels are cleared instead of showing guessed success. Pending fields and `modelBusy` clear when the current application finishes; model errors remain visible outside the popover. Prompt rejection clears pending intent.
- Workspace identity/eligibility changes, runtime restart (including resource-choice changes), and provider disposal invalidate pending work. Generation, runtime session and catalog token prevent late completion from updating a replacement runtime. View replacement preserves host-owned pending/in-flight settings and resynchronizes the new view; old-view actions are rejected.
- Session completion uses `agent_settled`: no remaining automatic retry, compaction retry or queued continuation. `turn_end` or low-level `agent_end` is insufficient. Use the [pi integration guide](../guides/agent/pi-integration.md) for current-version and upgrade evidence; historical WI-004 observations apply only to their recorded version.

## Trust and lifecycle

- `PiChatViewProvider` owns the in-memory workspace identity, monotonic generation, choice and subscriptions. It observes folder changes and trust grants; every action re-reads actual workspace folders, `workspace.isTrusted` and `env.remoteName`. Remote hosts are blocked even with file URIs. Multi-root and non-file workspaces are blocked. Only a trusted single local file folder permits a choice.
- Workspace changes or eligibility changes reset choice and advance generation; repeated reads and view rebuilds retain choice. Extension-host reload resets everything. VS Code trust revocation reloads the window; the host also detects trust loss on every request. No settings, trust files, secrets or webview storage are used.
- `openFolder` is allowed only in an empty local window (`no-folder`). It opens a native single-folder picker, then rechecks generation and eligibility before calling public `workspace.updateWorkspaceFolders(0, 0, { uri: selected })` with the native file URI. Cancellation leaves state unchanged; a false return or exception shows a bounded retry / File > Open Folder recovery hint. A true return keeps the action busy until authoritative workspace change or host restart, preventing repeated updates; it is only request acceptance: the UI never fabricates a folder or successful state. Adding the first folder can restart the extension host without a folder-change event; the new provider constructor reads authoritative workspace state and starts with no resource choice. `manageTrust` is allowed only for an otherwise supported untrusted folder and invokes only `workbench.trust.manage`; it never grants trust itself.
- The editor-title navigation icon and Command Palette command `pi-vscode.focusChat` reveal the existing Pi container, then focus `pi-vscode.chat`; they do not toggle visibility or create another panel. The default remains `secondarySidebar`. The editor-title entry uses separate light/dark Pi SVGs; the Command Palette remains available when no editor title is shown.
- Native actions are serialized per active view. Pending work is invalidated on workspace change, view replacement/disposal or provider disposal. Results are checked again before subsequent commands or state updates. Failures show a fixed retry hint; cancel is not an error. Post failures are handled without leaking exception data; reopening the view resynchronizes.
- Allow/decline is editable and memory-only until workspace identity or eligibility changes. After a choice in an eligible workspace, the extension host starts pi in RPC mode with public `--approve` or `--no-approve` for that run only; changing choice restarts runtime. WI-010 replaces historical `--no-tools` startup with the controlled allowlist and exclusively bundled gate described above. Either choice disables third-party extension discovery; resource consent does not override it. Decline can still permit AGENTS.md context and other user/global resources per upstream docs. Neither is a sandbox or tool authorization.
- WI-004 chat: when `runtime === "ready"`, the host may send a bounded `prompt` over RPC and project `text_delta` events into `messages` until session-level `agent_settled`. The webview never imports pi, spawns processes, or sends arbitrary RPC. Transcript is memory-only; workspace identity/eligibility changes, runtime stop/restart or provider disposal clear `messages` and reject stale `sendChat` and late RPC events for prior runtime sessions.
- View listeners are disposed on replacement/disposal; workspace listeners on provider disposal. Each page gets a fresh script nonce and restrictive CSP. Names and paths arrive only in state messages and are rendered with `textContent`, never interpolated into HTML. Native buttons have clear text labels, keyboard activation and visible focus; state/errors use live regions.

## Planned T014-01 single-file attachment contract

**T014-01 baseline implemented and executably verified; maintainer acceptance remains separate in ACTIVE.** Host capture/draft/admission/history, v2 UI and bounded adapter writes form the existing baseline. The T014-02 section below supersedes only this baseline’s changed-source reattachment rule; other constraints remain binding. This section freezes the proposed first slice of [REQ-003](../product-requirements.md#req-003--explicit-editor-context). [ACTIVE](../../ACTIVE.md) owns approval; the existing [candidate sequence](../discussions/2026-09-22-pi-compatibility.md#wi-014-local-candidate-slices-2026-09-22) is unchanged. One whole workspace file, optional complete preview, removal, one submission and accessible immutable live-session history are included. Changed sources require reattachment; latest-content confirmation, selections, multiple attachments and advanced history navigation remain T014-02–05. Names below describe the implemented baseline fields.

### Ownership, eligibility and bounded capture

`DraftSubmission` owns the host draft, attachment preparation and submission ledger; `PiChatViewProvider` supplies execution eligibility and coordinates runtime/Stop/session transitions. `runtimeLifecycle.ts` owns the narrow internal prompt/result contract; adapter owns encoding, correlation and bounded writes; Webview owns presentation only. Use VS Code public document/picker APIs and host-only Node `fs/promises`/`path`, never Webview filesystem access, a generic bridge or pi session files. No new dependency, framework, process model or persistence.

- Add uses `window.showOpenDialog` with files only, single selection and current workspace as default. Cancellation is `cancelled`, not an error, and preserves the draft. Add is disabled while the one slot is occupied; remove then reattach explicitly. Require current trusted single local file workspace and ready controlled runtime. Recheck eligibility after every awaited boundary. No inferred open-file attachments.
- Before document loading, reject non-`file` URI, authority/query/fragment, nonabsolute or ambiguous Windows path (device/extended prefix, UNC, drive-relative, ADS, control characters, reserved device components, trailing dots/spaces). Resolve root and target with `realpath`; require a regular file and segment-aware `path.relative` containment, not a string prefix or blanket lowercase comparison. Check lexical and resolved path shapes; symlink/junction targets outside root fail. Record canonical root/target and stat identity (`dev`, `ino`, size, mtime/ctime); repeats must match. Unsupported/unresolvable identity fails closed. This is not an atomic filesystem lock or a malicious-workspace sandbox.
- Guard known credential sources **before loading**: any `.local-env` or `.ssh` component, `.env`/`.env.*`, `auth.json`, `credentials.json`, `id_rsa`, `id_ed25519`, and `.pem`/`.key`/`.p12`/`.pfx` files (case-insensitive component checks, both lexical and canonical). Never source SecretStorage, process environment or pi credential stores. After capture, reject recognizable private-key blocks or credential assignments using the existing credential-like key vocabulary (`api_key`, `authorization`, `password`, `secret`, `access_token`, with case/separator variants); no override or raw offending text in errors. This is conservative defense, not guaranteed secret detection in arbitrary code; UI warns users to attach only nonsecret code. All fixtures use dummy markers, never private files/keys.
- Check `stat` before `openTextDocument`: unopened disk file over **1 MiB** is `source-too-large` and is never opened/read by this feature. This acquisition ceiling allows common UTF-16/UTF-8 decoding overhead for a 256 KiB text snapshot; it is not the attachment budget. Reuse an already-open matching TextDocument (including dirty text) even if disk is larger, but still require existing regular-file containment. Before `getText()`, use `offsetAt` at the last line end to reject over **262,144 UTF-16 code units** without allocating the entire text; then enforce **262,144 UTF-8 bytes** on captured text. For unopened files, VS Code owns decoding/loading; its public API offers no hard read quota against concurrent file growth. Re-stat afterwards and reject changes/oversize; state this residual race rather than claiming bounded OS reads. No raw fallback read, automatic save or stale disk substitute.
- Text eligibility means a supported file successfully opened as a VS Code TextDocument; rejected/binary/unsupported opens become `not-text`. This is not an extension-based binary sniffer or a guarantee that arbitrary bytes are text. Empty text files are allowed with a nonempty user body. Record actual editor text, document object/URI, version and dirty flag. Change/close/reopen, rename/delete, changed canonical/stat identity, or a different final text invalidates the attached revision permanently until removal/reattachment—even if text is later restored. Observe document changes/close and revalidate on send; `DraftSubmission` owns and disposes its document listeners. Previews always show the captured snapshot, never silently refresh it.

### Versioned intents and safe projections

Migrate the **entire Webview envelope to numeric version 2** in the same slice: validator, all current UI senders/host projections and their tests. Existing actions retain their payload semantics except draft-based `sendChat` below; reject all version-1 actions rather than letting an old page bypass attachment admission. HTML and host ship together; old pages must reopen/reload, with no compatibility shim. Every v2 action other than bootstrap `ping`/`getWorkspaceState` adds `viewId` (host-issued opaque ID) beside existing `generation`; host still checks the actual view instance. Bootstrap replies bind current `viewId`. This does **not** change the distinct `pi-vscode-approval` gate protocol v1 or upstream RPC.

All objects use exact own data fields/no accessors. IDs are `[A-Za-z0-9_-]{1,100}`; revisions and offsets are nonnegative safe integers, never wrap/reuse. The common action envelope is `{ version: 2, type, generation, viewId }`; the following lists **all additional fields**:

- `updateDraft`: `{ draftRevision, editSequence, text }`; text at most **8,000 UTF-16 code units before trim**, empty allowed for editing. One update in flight; UI retains only the latest local edit while waiting. Host requires matching base revision and increasing view-local `editSequence`, stores text, advances revision and returns accepted sequence. Send waits for latest edit acknowledgement. No attachment text/path is accepted from UI.
- `addFileAttachment`: `{ draftRevision }`; `removeAttachment`: `{ draftRevision, attachmentId }`; `sendChat`: `{ draftRevision }`. Host retrieves body/attachment itself; body must be nonempty after trim and stays independently 8k-bounded. Plain-text sends retain trimmed-body behavior and upstream command handling; enriched sends use the encoding below. Each mutation advances host revision; late/duplicate base revisions only resynchronize.
- `getAttachmentPreview`: `{ requestId, snapshotId, offset }`; offset is a UTF-16 boundary in a current draft or retained submission snapshot. One active preview response per view, no pending queue; excess valid requests get `busy`. Fixed maximum chunk **16,384 UTF-16 units**, at most **49,152 UTF-8 bytes**, avoid splitting surrogate pairs; next offset is host supplied. Full preview is optional and sequentially assembles at most one 256 KiB snapshot in the UI.
- `getAttachmentHistory`: no additional fields. Returns the complete bounded metadata list, not bodies; thus every retained snapshot remains reachable even after its chat row leaves the 32-message projection. No pagination subsystem in this slice.

Dedicated `attachmentState` (v2, current generation/viewId) contains `draft: { revision, text, acceptedEditSequence, attachment }`, `preparation: "idle" | "picking" | "preparing"`, `result: null | { code }`, `historyCount`, `retainedBytes`, and `lastSubmission: null | { submissionId, draftRevision, delivery, outcome }`. `attachment` is null or `{ attachmentId, snapshotId, relativePath, kind: "file", utf8Bytes, unsaved, state: "attached" | "changed" | "unavailable" }`. No absolute source paths/document objects/stat data enter attachment DTOs. Metadata path is at most **1,024 UTF-8 bytes**; oversized metadata rejects capture, not silent truncation.

`attachmentHistory` adds `entries` of `{ submissionId, snapshotId, relativePath, kind: "file", utf8Bytes, unsaved, delivery, outcome }`; maximum **128 entries**, each serialized metadata object at most **2 KiB UTF-8**. `attachmentPreview` contains `{ requestId, snapshotId, offset, nextOffset, done, text }` or `{ requestId, code }` (discriminated by presence of `code`); full serialized response including envelope is at most **128 KiB UTF-8**. These safe text DTOs render with `textContent` only, never HTML, Markdown or executable links. View/snapshot/request mismatch discards replies. State/history refresh only on relevant changes/request/recreation, not every streaming delta; at most one assembled preview is kept, replacing/closing it frees its text.

Codes are exactly `cancelled`, `busy`, `stale`, `ineligible`, `slot-full`, `invalid-source`, `outside-workspace`, `unavailable`, `not-text`, `source-too-large`, `text-too-large`, `metadata-too-large`, `sensitive-source`, `source-changed`, `history-full`, `frame-too-large`, `preparation-cancelled`, `write-failed`, `ack-timeout`, `rpc-rejected`, `runtime-lost`. Malformed envelopes remain ignored without response/effect; valid obsolete actions return current state with `stale`. UI maps codes to fixed recovery text (at most 300 characters), never exceptions/content. Source/budget failures preserve the draft and offer remove/reattach/reduce; history-full offers inspect history or deliberately end/restart the live session with loss warning; transport uncertainty offers Stop/restart guidance, never automatic retry.

### Draft transaction, delivery and lifecycle

1. Host owns acknowledged body and attachment across view recreation. New view gets fresh `viewId` and current revision before editing; local newer unacknowledged text is not overwritten by an old acknowledgement. UI clears only on `lastSubmission.draftRevision` matching the submitted revision **and** unchanged local edit sequence; new edits are subsequently synchronized as a new revision. Changes during preparation invalidate its token and preserve the newer draft. An edit never received by host cannot be promised after view loss; UI labels synchronizing/unsynchronized state and does not claim it is saved.
2. Serialize one picker/preparation and one submission. Capture preparation token, draft revision, workspace generation, runtime session and current view. Perform bounded async source checks; recheck every identity after awaits. At final synchronous boundary verify document object/version/open status/text/dirty flag, body/attachment budgets, metadata, eligibility and cancellation token; reserve history capacity, construct one immutable submission, serialize and validate the actual outbound frame **before admission**. No asynchronous file reread after freezing. This guards host/editor consistency, not future runtime tool reads or check/use races.
3. Admission atomically commits the immutable snapshot plus body, advances/clears only that draft revision and marks delivery `host-accepted`; history and prompt take the same frozen data. Call the adapter immediately within the same synchronous turn; adapter checks expected session and consumes its one-attempt token before handing bytes to stdin. `write-attempted` is published at that boundary, `rpc-accepted` only on matching successful response, `rpc-rejected` only on an explicit negative response, `not-sent` only when no write was attempted, and `unknown` on write/ACK/loss uncertainty after an attempt. No reinsertion of a possibly sent draft and no automatic resend. Keep even rejected/uncertain submitted snapshots for inspection, accurately labelled—not all are “sent successfully”.
4. Task outcome is independently `pending | settled | failed | interrupted`; matching `agent_settled` is the existing settlement boundary, not the prompt ACK. Early streaming/settlement before ACK must not regress outcome or allow a second admission while ACK remains unresolved. Host acceptance or write/drain is not remote acceptance/completion.
5. Stop during picking/preparing invalidates token, yields `preparation-cancelled`, preserves draft and guarantees zero prompt writes. After write handoff, use existing controlled Stop/clear_queue/abort; remote effects may already exist. Stop never sends drafts, restores uncertain submissions or replays them. View disposal cancels uncommitted picker/preparation/preview but preserves acknowledged draft and admitted history; a committed attempt continues host-owned. Old-view events cannot mutate the replacement.
6. Workspace/trust/eligibility or runtime replacement invalidates attachment eligibility and async work; retain body with an invalidation notice, remove source capability, require deliberate reattachment, and never silently carry context to another project/runtime. Live history ends only with its owning session: disclose memory-only loss on deliberate restart/resource-choice change; fault loss reports history ended, not persistence. Host reload/provider disposal releases all snapshots, subscriptions, timers and preview references. A same-session successful Stop retains history. No new session persistence or automatic reset.

**Delivery/settlement recovery:** an explicit RPC rejection ends the waiting execution state as failed and offers configuration inspection before a deliberate new submission, not an unavailable Stop action. If settlement precedes the ACK, admission remains blocked until the ACK resolves, then the same settlement projection completes the turn. A late ACK cannot overwrite runtime-failure/interrupted state. A valid Stop received after settlement still republishes current state so a renderer cannot remain locally stuck in stopping.

### Encoding, transport and memory budgets

Installed and declared pi are **0.86.1** (read-only inspection, not runtime evidence). Its public `docs/rpc.md` and `dist/modes/rpc/rpc-types.d.ts` define `prompt.message`, images and streaming behavior, **not** an RPC `expandPromptTemplates` option. Do not invent that RPC field from an SDK option or import private types at runtime. For attachment submissions construct `message` as the fixed non-slash ASCII prefix `User task with explicit untrusted file context. JSON data follows:\n` plus `JSON.stringify({ body, attachment: { path, kind: "file", unsaved, text } })`. This makes the entire prompt non-command-leading; body/file slash/template text stays JSON data, with delimiters/control characters escaped. Verify literal template/skill-command fixtures in the later authorized real-runtime check; no assertion that labeling defeats prompt injection. No-attachment prompts keep existing semantics.

- Body raw length ≤8,000 UTF-16 units, attachment text ≤262,144 UTF-8 bytes, path ≤1,024 UTF-8 bytes. Fixed prefix/JSON keys, boolean, punctuation and RPC envelope/ID must occupy at most **1 KiB** before escaping. Non-body/non-text metadata plus framing is rechecked in serialized output; final **outbound prompt JSONL frame ≤4 MiB UTF-8 including LF**. Other input/projection bounds are not enlarged. Adapter receives a discriminated host-only plain/enriched prompt input and independently checks the body and attachment caps plus serialized frame; do not bypass the 8k body validator with an arbitrary large `prompt(string)` path.
- Analytical conservative bound, not an executed test: JSON can expand a one-byte ASCII control to six ASCII bytes; nesting that JSON inside RPC can at most double those bytes. Text contribution ≤`12 × 262144 = 3,145,728`; body ≤`12 × 8000 = 96,000` (lone-surrogate escapes also fit); path ≤`12 × 1024 = 12,288`; allow `12 × 1024 = 12,288` for fixed overhead. Sum **3,266,304 bytes < 4,194,304**. Still measure `Buffer.byteLength(serializedFrame, "utf8")` before every actual write; this proof is for one file, not the final 1 MiB multi-file target. Existing receive cap of 8 Mi characters is unrelated.
- No attachment write queue: one prepared frame, one write attempt. Register response/stream error/close handlers before write; absent/closed stdin or a serialization failure before invocation is not-sent; a throw/error once `write` is invoked is conservatively unknown, including callback error. Wait for successful write callback and, if `write()` returned false, `drain`; allow an early correlated ACK but do not leak listeners or reorder a later prompt. **5 seconds** for local write/drain, **30 seconds** total ACK deadline from write invocation (not an additional 30 seconds). On timeout/error/loss clean listeners/timers/pending response, fail current runtime through the existing failure/shutdown path, no retry. Stop control writes retain their existing bounded cancellation semantics and must not wait behind a product queue of attachments. Tests must exercise ACK before callback/drain, close without drain and Stop racing write. No claim of remote delivery from local flush. Transport-loss cleanup binds the captured child identity even after session invalidation; shutdown detaches it before awaiting closure so late callbacks cannot terminate a replacement. SIGTERM escalates after 5 seconds; absence of observed closure after another 5 seconds reports unconfirmed shutdown and blocks replacement until manual cleanup and host reload.
- Retained live submissions have **8 MiB total UTF-8 logical payload** (body + attachment text + serialized immutable snapshot metadata: submissionId, snapshotId, relativePath, kind, utf8Bytes and unsaved). Delivery/outcome are bounded record-state overhead, not logical snapshot payload, **128 records maximum**. Reserve exact charge before admission; no silent eviction or perpetual global quota—capacity resets only when the live session ends, with disclosed loss. The record cap bounds empty/tiny-file metadata and allows history beyond 32 visible messages; the byte cap allows many full-size snapshots without unbounded growth. Whole bounded history list is accessible from an explicit “Attachment history” control from the first slice; T014-05 improves navigation, not basic access.
- Memory is separate from wire size: retained strings have a conservative **16 MiB UTF-16 content ceiling** under that 8 MiB charge, plus bounded 128-record/object overhead (not a heap-size guarantee). One draft ≤256 KiB text plus 8k body; one preparation snapshot; one ≤4 MiB UTF-8 outbound buffer with transient serialized strings released after write; one preview chunk and one UI assembled preview ≤256 KiB UTF-8. Do not cache duplicate serialized prompts per history entry or include history bodies in every workspace projection. Overflow blocks new attachment sends but not plain chat, Stop or inspecting retained data.

### Reproducible keyless runtime verification (2026-09-22)

Explicitly authorized T014-01 evidence: run `npm run compile`, then `node scripts/spikes/spike-attachment.mjs --build`, then `wsl.exe -d Ubuntu-24.04 --user nobody --exec unshare --user --map-root-user --net /usr/local/bin/node /mnt/d/Users/hex4c59/Projects/Typescript/pi-vscode/scripts/spikes/spike-attachment.mjs`. The helper bundles the actual production adapter; only process setup adds the reviewed `attachment-provider.mjs` public `registerProvider`/`streamSimple` fixture. It exercises production preparation, encoding, stdin write/drain, ACK, one-attempt token and shutdown against genuine pi 0.86.1 RPC. Synthetic inference captures the last real model-input message and returns fixed text; it is not real-provider quality/context-window evidence.

The 2026-09-22 21:57 Windows-launched WSL run passed: Node v24.12.0, namespace root mapped only to nobody/65534, no interfaces or usable routes, fresh allowlisted environment/home/config/workspace, no session persistence, discovery disabled except the explicit bundled gate and reviewed fixture. An enabled `/literal` template positive control expanded; enriched leading slash/skill/template text remained exact JSON data. Three 262,144-byte attachments (Unicode, NUL controls, quoting/newline text) produced measured LF-inclusive frames of 262,425 / 1,835,289 / 334,745 bytes. Provider input matched the frozen pre-mutation payload exactly; each token attempted once, replay was rejected, and 262,145 bytes caused zero additional writes. Owned RPC close was observed as code 143 after adapter SIGTERM (not a natural code-0 exit); worker exited 0 and fixture cleanup passed. No user secrets, network or paid inference were used. This is Linux adapter/runtime evidence, not native Windows runtime, F5 or installed-VSIX acceptance; ACTIVE retains those gaps.

### Required slice acceptance and evidence

After one explicit consolidated Build approval: ordinary owner-local specs extend `src/extension/tests/harness.ts` with narrow native picker/document/version/stat seams; capture exact runtime input and observable state, no new E2E runner. Test valid/dirty/empty capture without save, cancellation, binary/open failure, pre-open stat ceiling, already-open small dirty snapshot over a large disk file, lexical/canonical Windows escapes, source identity/version changes twice, close/reopen/delete, blocked sensitive dummy sources, and final growth/metadata overflow. Assert **zero writes** for rejected preparation and exact immutable history/prompt text for admission.

Validator tests cover all v2 actions, raw body 8,000/+1, no path/text authority, old v1, accessors/extras, IDs, revisions, offsets and stale views. Budget/adapter specs cover ASCII controls, quotes/backslashes, Unicode/lone surrogates, 256 KiB/+1, actual nested frame bounds/LF-only fragmentation, wrong-session ACK, callback/drain errors/timeouts/close, duplicate send, Stop and replacement at every await, and listener cleanup with no retry. Mounted React/jsdom UI tests cover safe literal complete preview, chunk assembly, remove/reattach, history beyond 32 rows, both retention caps, independent newer draft edits, recreation/late ACK, empty/loading/error/stopping states and stable focus/expansion/scroll during deltas. Retain model/thinking, approval and plain-chat regression assertions; mocks do not prove real pi behavior.

Run `npm run compile`, `npm run lint`, `npm test`, `npm run docs:verify` and `git diff --check` for the eventual Build. This documentation task runs only documentation checks. Windows VS Code F5 and **installed VSIX** must separately verify native pick, dirty editor/no save, changed-file reattachment, 256 KiB literal preview/send, history/recreation and keyboard/focus/themes/Stop. Cursor evidence stays separate. A later explicitly authorized isolated runtime check must verify actual pi framing/literal command handling without private data; real models/network/credentials/paid calls are **not** included in Build approval. Missing real-host/runtime evidence keeps that acceptance pending, not a new design ticket or a reason to claim success. No gate/ADR/whole REQ-003 acceptance follows automatically.

## T014-02 latest whole-file confirmation

**Scope:** REQ-003, authorized by the long-running Goal recorded in [ACTIVE](../../ACTIVE.md), implemented only when WI-014 is the sole Build. This extends T014-01; its one-file, capture, transport and live-history bounds remain unchanged. It does not authorize selections or mixed collections in this slice.

- Before each enriched Send, the host captures current editor text again under the same canonical containment, source eligibility, sensitive-content and UTF-8 limits. A different canonical target or editor-document identity is unavailable, not a silently substituted file. Changed content, editor version/dirty state or filesystem identity blocks submission and replaces the draft preview with a new opaque snapshot ID and state `confirmation-required`. The draft text is retained; no runtime prompt or history record is created.
- Add the exact v2 intent `confirmFileAttachment { draftRevision, attachmentId, snapshotId }` to the ordinary generation/view envelope. All identifiers and the revision use existing validation bounds. The host accepts only the current staged whole-file snapshot while idle and eligible; it revalidates its source asynchronously before marking it `attached` and incrementing draft revision. Confirmation never sends a prompt by itself; the user deliberately presses Send afterward. Preview is optional and never grants confirmation. There is no old-whole-file send option.
- Document edits invalidate the staged/confirmed state. A later Send stages the new current snapshot and again requires explicit confirmation. Stale IDs/revisions, newer draft edits, remove, Stop, view replacement, workspace/runtime replacement and disposal cannot authorize or revive an old confirmation. Stop during preparation cancels it while preserving the draft. An unchanged staged snapshot remains inspectable after a view rebuild, without replaying a confirmation.
- The React client labels the staged state, offers **Use latest contents** only for that exact snapshot and emits no paths/text authority. The action is disabled during synchronization, unsent draft acknowledgment, model/task work or preparation. After a changed snapshot projection, an open preview for the replaced draft snapshot is closed rather than presenting old bytes as current.
- Existing agreed seams are the actual provider/validator with native-editor/runtime harness, the mounted React app/real bridge, and the isolated genuine pi transport plus F5/installed VSIX. Exercise two successive edits, confirm without preview, duplicate/stale confirmation, changes during validation, over-limit/deleted/unavailable sources, Stop/recreation and exact final submitted/history bytes. Historical T014-01 real-host evidence does not establish this new behavior.

## T014-03 fixed selection and explicit old-snapshot choice

**Implemented contract; executable evidence and remaining acceptance are recorded in [ACTIVE](../../ACTIVE.md).** REQ-003 under WI-014’s Goal authorization. T014-02 remains in force; this slice keeps one attachment slot while adding the selection kind. T014-04 owns mixed collections.

- `addSelectionAttachment { draftRevision }` uses the existing v2 action envelope. Host reads the active local text editor's single nonempty selection; absent editor, empty/multiple ranges, ineligible source or cancellation produces a bounded recoverable code. No path, range or selected text is accepted from the Webview. Capture only selected text, not the entire document, so a small valid selection in a large already-open document remains possible. Apply existing canonical/path/content safety and 256 KiB UTF-8 item bounds, with source/document checks before and after capture; never save.
- Metadata and enriched context gain discriminated `kind: "selection"` with `originalRange { start: {line, character}, end: {line, character} }` (zero-based safe integers, end-exclusive) and `stale: boolean`. Browser labels use one-based original positions, explicitly historical rather than current navigation. File-kind metadata remains unchanged. The captured selection text, original range and original unsaved flag are immutable until remove/reattach; changing the cursor alone does not move the attachment.
- The host tracks a separate currently authorized source revision for selection submission. Source edits invalidate it without updating the captured text. Send checks current source eligibility/document identity and shows the stale choice, with zero submission, when the observed source version differs. UI offers remove/reattach and explicit **Use old snapshot**; preview is optional and inert. `confirmSelectionAttachment { draftRevision, attachmentId, snapshotId }` validates exact fields and the currently observed source revision; another change before/during confirmation or final Send requires the choice again. Confirmation alone never submits. Deleted/unavailable/outside sources cannot be bypassed by old-snapshot choice.
- Confirmation token/revision invalidation, Stop, newer draft edits, view/runtime/workspace replacement and immutable submitted history follow T014-02. Old range labels and actual submitted bytes persist in live history; no saved-session storage is introduced. Existing frame, preview, live-memory and record bounds remain enforced including new metadata.
- Agreed seams remain native-editor/provider/validator, adapter serialization, mounted React/bridge and isolated genuine pi plus actual F5/installed VSIX. Verify exact start/end (including Unicode, multiline and dirty text), small selection in a large document, no/empty/multiple selection, changed source versus changed cursor, stale confirmation/repeated edit/Stop/rebuild, refusal after deletion/escape and unchanged historical bytes. Keep whole-file latest-content and model/approval/Stop regressions. No broader multi-range, multi-file, image or persistence behavior is implied.

## T014-04 bounded mixed attachment transaction

**Implemented contract; current executable evidence and remaining maintainer acceptance are recorded in [ACTIVE](../../ACTIVE.md).** This replaces only the single-slot restriction in T014-01–03. [REQ-003](../product-requirements.md#req-003--explicit-editor-context) owns the product limits; owners, trust and memory-only lifetime remain unchanged.

- The paired v2 host/browser projection changes the draft field to `attachments: DraftAttachment[]`, ordered by explicit addition, with 0–20 entries. Existing named add/remove/confirm/preview intents continue to carry only revision and opaque IDs. Whole-file picking may select multiple eligible files; a cancelled, unsafe, over-count or over-budget batch leaves the existing list unchanged. Selection addition appends one fixed snapshot. No inferred context, reordering feature or new storage is introduced.
- Host checks per-item 262144 UTF-8 text bytes, aggregate 1048576 text bytes and count 20 at acquisition and again for final content. Count and aggregate failures use bounded `attachment-limit` / `total-too-large` codes with reduction/removal guidance. The separate 8000 UTF-16-unit body limit stays. Every mutation advances the draft revision and invalidates pending preparation. Exact per-item confirmation cannot authorize another item or send the draft.
- Send prepares the complete set and revalidates every item's document/source and confirmation state before one admission. Changed whole files stage latest candidates; changed selections retain original bytes/ranges and require old-snapshot choice. Any changed, unavailable, unsafe, excessive or unconfirmed item means zero prompt writes, no partial history and no implicit omission; the body/list remain available to fix or remove. Stop, newer drafts, list changes, view/runtime/workspace replacement suppress all late acquisition/confirmation/admission results.
- Enriched adapter input and literal JSON context use `attachments: [...]` in that same order, not separate prompt calls. The adapter independently checks item/count/aggregate/path/range/body limits and the exact serialized JSONL frame. The frame ceiling increases from 4 MiB to **8 MiB** for the approved 1 MiB text aggregate, including nested JSON escaping; worst-case escaping must have actual transport evidence. Oversize frames fail before stdin writes; no truncation or replay.
- Live history remains bounded to **128 attachment snapshot records and 8 MiB logical payload**, without eviction. A mixed submission shares one submission ID and delivery/outcome across its immutable records; preview still addresses each unique snapshot ID. Charge the user body once per submission plus each snapshot's exact text and immutable serialized metadata (existing 2 KiB per-record metadata bound), and reserve capacity for the whole set before admission. History count remains snapshot-record count; this does not silently expand the existing retention policy. Navigation improvements belong to T014-05.
- Agreed host/validator, adapter, mounted React/bridge and genuine-pi synthetic/native seams verify one mixed vector, 20/21 items, 1 MiB/+1, Unicode/escape bounds, all-or-nothing batches/sends, multiple changed-item confirmations, source edits during asynchronous preparation, stale answers/Stop/recreation, complete previews and exact retained/delivered bytes. F5 and installed VSIX evidence remain separate from browser and model-quality evidence.

## T014-05 bounded live-history navigation

**Implemented contract; executable evidence and remaining maintainer acceptance are recorded in [ACTIVE](../../ACTIVE.md).** REQ-003 owns immutable sent context; REQ-005 supplies cancellation constraints. This is not REQ-008 saved-session restoration.

- Keep the existing host-owned 128-record / 8 MiB no-eviction retention and complete bounded metadata-only `getAttachmentHistory` projection. Paginate that projection locally at **16 snapshot records per page**, in admission order; show the visible record range, total, current page, and submission grouping/ordinal without treating opaque IDs as source links. A submission may continue on another page; do not imply omitted attachments were never sent.
- Opening history starts at the latest page. First/previous/next/latest controls are named and bounded; subsequent metadata/status updates and streaming deltas do not silently move the selected page. If a live-session loss empties history, reset navigation honestly. View recreation may reset ephemeral page/scroll UI but must recover host-retained acknowledged draft, metadata and exact previews in the same live session.
- Retain on-demand complete literal previews, existing 16,384 UTF-16 / 49,152 UTF-8 chunk limit, one active response per view and one assembled 256 KiB snapshot. Moving pages or closing history cancels an open history preview locally; unmatched late chunks cannot reopen it. No snapshot body is sent with page metadata or each delta. Draft preview/capture/confirmation/send rules stay unchanged.
- Pagination, preview and status refresh never submit, rewrite or clear the draft. Preserve page selection, keyboard focus and the context scroll position during ordinary streaming/status updates. Render at most one metadata page; no new pagination RPC, persistent store or generic virtualization framework.
- At `history-full`, keep the complete unsent draft and retained history. Give concrete optional recovery guidance: inspect history, or deliberately use **Developer: Reload Window**, warning that current chat, attachment snapshots and unsent draft are lost. Do not execute/reset automatically or imply persistence; budget, source and transport failures retain their distinct recovery meanings.
- Verify page boundaries/empty history, mixed submission continuation, full-preview integrity after source changes/deletion and after more than 32 chat messages, host/view/runtime loss, stale chunks, Stop, budgets and keyboard/scroll stability. Use the approved host/client/React seams plus browser and separate F5/installed synthetic-runtime checks.

## Planned WI-013 local interaction contract

**Paused on 2026-09-22, not completed:** retained for future resumption; [ACTIVE](../../ACTIVE.md) now prepares WI-014 attachments. This candidate is not an attachment transport contract and its sizes below must not be applied as global prompt/attachment bounds.

**Candidate only — not the current version-1 allowlist, a Living contract or Build authorization.** On 2026-09-22 the maintainer selected route 1 (this local candidate) with route 3 (a local unpublished upstream proposal); route 2 needs a separately selected narrow pilot. [Draft ADR 0002](../decisions/0002-interaction-contract-route.md) records route approval, not acceptance of every field or budget. The document stays Outline. Existing controlled-chat behavior above is unchanged; the following semantics concern future standard interactions under REQ-004/005/006/009. The later September 22 amendment in ADR 0002 makes unmodified released pi the baseline and upstream enhancement optional. Remote gaps block dependent claims only, not the project as a whole. Evaluate the current-release slice with honest local evidence; remote close/outcome/operation fields below are conditional capabilities, not mandatory new-protocol adoption by every extension. The [investigation](../discussions/2026-09-22-pi-compatibility.md) owns pinned pi 0.86.1 evidence and the upstream proposal.

### Ownership and candidate discriminants

Names below describe a reviewable local schema, not exported TypeScript types or pi wire fields. Before Build, freeze the exact envelopes, errors and version/capability migration with tests; do not silently extend current `version: 1` consumers.

- **Host:** sole owner of admission, operation eligibility, the in-memory interaction ledger, queue, local deadlines, projection and recovery barrier. Each entry binds host epoch, workspace generation, runtime instance/live-session identity, opaque non-reused interaction ID and current view revision. Upstream IDs stay private to the adapter; a UI ID never grants tool permission.
- **Adapter:** owns validated transport correlation, a one-shot reply token per admitted remote request, method-specific mapping and passive owned-child observations. It translates evidence, not product readiness. Host revokes eligibility first; adapter independently consumes a token before any write to guard duplicate calls. Cleanup belongs to the creator of timers/listeners/tokens. Runtime owns remote dialog and execution facts; neither local owner invents them.
- **Operation association:** discriminated `association: { kind: "runtime-scoped", operationId, coverage } | { kind: "reviewed-fixture", evidenceRef } | { kind: "unknown" }`. `runtime-scoped` requires verified public capability bound to the current connection; coverage is `awaited-handler | registered-work | unknown`. A reviewed fixture is limited source-review inference, not general support or an enabled route-2 pilot. Unknown attribution cannot admit operations whose support depends on reliable separation. Host-assigned correlation alone cannot establish provenance. Never infer source from title, notification, `hasUI` or one pending command; runtime attribution is not authentication against co-loaded executable code.
- **Adapter evidence:** separate event discriminants `interaction-opened`, `interaction-closed`, `response-outcome`, `operation-settled`, `transport-lost`, `child-exited`, `spawn-failed`. All bind the relevant runtime/session; operation/dialog events additionally bind their IDs and evidence basis. Close/outcome/settlement variants are emitted only when the selected remote capability supplies that fact. Legacy absence stays unknown, not a synthesized event. `operation-settled` includes `completed | failed | cancelled` and coverage, never blanket cessation of detached work.
- **Host ledger:** independent axes: local validity `queued | active | invalidated`; reply attempt `none | reserved | written-locally | write-failed`; remote dialog `unknown | closed` with evidenced reason/outcome; operation `pending | settled | failed | unknown` with coverage; owned runtime `connected | lost | exited | spawn-failed`. Local invalidation records a bounded reason such as answer-attempt, user-cancel, Stop, local-cutoff, remote-close, replacement or loss. An entry never becomes valid again. A later remote fact may refine evidence, not reopen it.
- **Webview projection:** candidate `interactionState` projects one method-discriminated form (`select` with opaque option IDs/labels, `confirm`, `input`, `editor`), identity/view revision, queue count, `canAnswer`, bounded literal text and evidence-derived status. Candidate `answerInteraction` carries only the identity/view revision plus `answer: { method: "select", optionId } | { method: "confirm", value: boolean } | { method: "input" | "editor", text: string }`; `cancelInteraction` carries identity/view revision only. Stop is a host-coordinated intent, not arbitrary RPC. These named intents are not admitted by today's validator. A negative confirm differs locally from cancellation even if pi maps both to false. Ordinary confirm cannot create or reuse a tool grant.

### Validation, finality and ordering

1. Validate plain objects, exact own fields, supported envelope version/discriminants, bounded identities, current workspace eligibility/runtime/view, active ledger entry and method before mutation. Select must match the original option membership; adapter maps the selected ID to its exact original value. Confirm accepts only boolean; input/editor accept bounded strings without coercion, silent trimming or truncation (empty string stays distinct from cancellation where the method permits it). Reject unknown/extra fields, wrong method, stale or duplicate answers without transport effects; a valid but obsolete UI resynchronizes. Answer payload budgets are checked again immediately before serialization.
2. Serialize terminal arbitration in the host. On answer/cancel, atomically invalidate the entry and reserve its single reply token before awaiting I/O; then attempt the method-specific response once if transport and scope remain eligible. A Stop/replacement/loss that wins before the write suppresses an unsent affirmative response. No retry after uncertain writes and no second cancellation response after an answer attempt. Duplicate intents cause zero additional writes. The guarantee is **exactly one local reservation and at most one write attempt**, not exactly-once remote delivery or execution; some invalidations produce no write at all.
3. On Stop, first block new tasks/admission and invalidate all active/queued answers plus covered pending approvals, then cancel still-unanswered dialogs through supported responses and request queue clearing/operation or agent cancellation as supported. An already-written answer may win remotely; Stop cannot retract it. Ordinary dialog cancellation can resume extension code. Keep stopping until relevant evidenced settlement or explicit unconfirmed failure; neither local write/drain, cancel/abort ACK, `agent_settled`, `promptInFlight=false` nor handler return alone proves arbitrary extension completion. Do not apply pending settings or switch profiles/sessions while unresolved.
4. Remote close invalidates the matching active or queued entry immediately upon observation; delayed close/outcome cannot mutate another scope. Identical duplicate facts are harmless; conflicting finality or missing required ordering fails the affected scope closed with a bounded diagnostic, not guessed success. Registered dialogs/children must be accounted for before a scoped operation can be treated as settled. Ordering depends on the negotiated evidence, not on inventing a global sequence for legacy RPC.
5. View replacement advances view revision and rejects old-page intents; it resynchronizes the same host ledger without restarting deadlines or replaying requests. Workspace/runtime/session replacement and provider disposal invalidate entries before detaching listeners/timers. Retain only the bounded evidence needed for stale rejection/diagnostics; old callbacks cannot update new identity. Draft retention and no automatic replay remain required. Existing WI-010 fault shutdown is unchanged; it is not authority to add automatic kill/restart for WI-013 incompatibility.

Candidate host error codes distinguish `invalid-message`, `invalid-answer`, `unsupported-method`, `source-unknown`, `unsupported-capability`, `overload`, `write-failed`, `transport-lost` and `stop-unconfirmed`. They describe the failing boundary, not upstream exceptions. Invalid input has no action; unsupported admission offers a limitation/terminal route; overload follows the queue policy below; write/loss uncertainty never triggers response retry; unconfirmed Stop holds the recovery barrier. Exact UI wording and error envelopes remain to be frozen before Build.

### Queue, timing and memory bounds

Present one active form and a bounded FIFO queue with visible count. Expire/close queued entries without presenting them; recheck validity/deadline at dequeue and reply. Keep admitted requests on overflow, reject/cancel extras only via supported responses and warn. Sustained overflow enters interaction failure, disables answers and blocks new tasks without claiming execution stopped or automatically restarting. If rejection cannot be sent, remote state stays unknown.

No upstream deadline means no blanket human countdown; hidden private signals may still exist. A declared duration starts upstream before output, not at sidebar display. Receipt plus duration is not the remote deadline or a proven remaining response window. A receipt-based local cutoff would be a separately approved, labeled presentation policy, never an extension of remote validity or a substitute for evidenced expiry. Never restart timing on dequeue or view recreation. Remote deadline metadata must define clock/elapsed-time semantics; late delivery is disclosed. Transport and Stop-settlement deadlines are separate from human response time; the current prompt-response timeout needs redesign before indefinite command dialogs.

**Unapproved sizing candidates only:** one active plus seven queued; 64 KiB UTF-8 complete frame (including partial-frame buffer bound), 32 KiB text/answer, 64 options each up to 1 KiB within the aggregate frame; five overflows in ten seconds as a sustained-overflow candidate. Lower peer bounds win. These numbers are not implementation defaults or accepted product limits; usability/transport evidence and approval remain required. Oversized inputs fail explicitly, never silently truncate an answer. Diagnostics exclude content/secrets and use bounded codes/counters with rate limiting.

**Replay bookkeeping is not a lifetime interaction quota.** The earlier 256-terminal-record suggestion does not authorize stopping a healthy connection after 256 dialogs. Candidate local strategy: fresh host/runtime epochs plus monotonic non-reused local IDs, a bounded live-token map and a bounded recent terminal diagnostic ring; an answer requires a live token, so evicting diagnostic text never restores answer eligibility. Counter exhaustion must not wrap/reuse IDs. Remote open-ID replay requires a separately proven non-reuse/sequence-watermark or tombstone policy at the adapter boundary; legacy arbitrary IDs do not supply it. Exact retention, remote duplicate handling and exhaustion policy remain pre-Build gaps, not an adopted consequential policy or unbounded tombstone set.

### Loss, exit and deliberate recovery

`transport-lost` means local answering is disabled and remote execution is unknown. A passive `child-exited` must identify the exact successfully spawned child and observed exit/close details; spawn failure or stream error is not exit evidence. Adapter must retain enough process-observation ownership after stream failure to observe a later real exit. This is a planned capability absent from today's merged loss event. Exit proves only that owned child ended, not descendants, side-effect rollback or operation success.

For already-pending unconfirmed Stop, host owns the recovery barrier: show “stop not confirmed / cannot continue”, preserve draft, block tasks/settings/profile/session transitions, and provide manual runtime-termination guidance. Actual owned-child exit and deliberate user recovery are separate conditions; a recovery click alone is not evidence. After sufficient boundary evidence and explicit recovery, any replacement would use fresh identities, controlled defaults and cleared grants without replay. Host reload loses ledger/child ownership; a new ready process, a PID absence check or user assertion cannot clear old-work uncertainty. How to preserve/enforce this barrier across ownership loss without new persistence or automatic termination is unresolved and **blocks that recovery branch and any selected slice relying on it**, not a claim that memory-only state already enforces it.

### Required evidence before implementation/acceptance

Not run for this addition: deterministic validator/ledger tests for every discriminant, option/string bounds, stale view/runtime/epoch, ID reuse/diagnostic eviction, duplicate/conflicting answers, both Stop/answer and expiry/answer orders, queued expiry, overflow, partial frames and write loss; assert no unauthorized effect and cleanup, not just call counts. Separately authorized pinned public-runtime tests must establish the selected slice's loading/consent and covered-approval boundaries, observable answer/cancellation effects, honest unknown outcomes, detached-work limits and child-exit versus loss/reload recovery. Source/scope mapping and enhanced terminal/cancellation evidence are required only for claims relying on those capabilities; they are not universal authenticated-source or exact-remote-completion requirements. Then Windows VS Code sidebar keyboard/focus/theme, F5, installed VSIX and a reviewed real extension remain required; synthetic/official-example evidence alone is insufficient. No new runtime capability, accepted ADR or gate closure follows from documenting these seams.

## Evidence boundaries

[WI-010 closure](../archive/2026-09-21-closed-wi-history.md#wi-010) owns the four development F5 checks and historical automated/integration/extracted-package results. The WI-008/WI-009 deferred-selection main path was confirmed at 19:44 on 2026-09-21; [ACTIVE](../../ACTIVE.md) owns its current handoff. That confirmation does not establish independent manual coverage of approval/Stop interleavings, recovery or the full grant/lifecycle/accessibility matrix.

Check existing F5 records for the particular native-dialog, trust/reload and host/fork scope being claimed; uncovered scenarios and installed-VSIX behavior require independent verification. The contract remains Outline and the three gates remain Open.

### WI-016 T016-01 — Dirty-editor write protection

Host policy checks built-in `write` and `edit` targets before offering an approval and again before allowing either a one-call decision or an exact live-session grant. It reads VS Code document metadata and filesystem identity, not buffer contents; it never saves a document. If the target has unsaved editor changes, the call is denied and the existing bounded `chatError` field tells the user to handle those changes and explicitly retry. An unreliable check also denies with a fixed recovery message. No new Webview capability or gate-envelope field is added.

The target mapping follows declared pi 0.86.1 write/edit path resolution (relative/absolute, one leading `@`, home, file URL, Unicode spaces, and native-Windows shell drive spellings). Existing files use canonical path / file identity; missing files use a canonical existing ancestor. The document set is rechecked after asynchronous resolution, with at most three attempts on churn. Non-file/closed documents are excluded. Unrelated dirty files do not prohibit clean targets or reads. Approving a call that became dirty while waiting creates no new session grant; existing grants do not bypass protection. Cancellation and grant revocation during a final check cannot revive authorization.

This is a public `tool_call` pre-execution check, **not an atomic lock** between an editor and a later filesystem write. Edits after the final check, opaque shell writes, trusted-extension execution and concurrent filesystem changes remain outside a complete-protection guarantee. pi still owns tool execution and cancellation. The UI shows a reason for the denied call; no retry, rollback or auto-save is performed. Current tests and native evidence belong to ACTIVE, not an implied REQ-007 review-diff completion.

### WI-016 T016-02 — Already-applied change review (Build contract)

This implements the Goal-approved REQ-007 slice recorded in ACTIVE, not a new PRD/ADR acceptance. Webview sends only v2/viewId/generation named intents: `getChangeReview` (no payload), `openReviewDiff` / `openReviewSource` (opaque host-owned `id`). Unknown/stale IDs cannot open arbitrary paths or commands. Host emits a separate `changeReviewState`; `webviewProtocol.ts` owns its exact DTO. Entries identify tool-reported write/edit targets versus workspace events observed while a task is active. Observation is not agent-only attribution.

Before/after text remains host-owned and is opened via readonly virtual documents in VS Code's native diff editor; no snapshot text, path capability, process, SDK or generic command bridge enters the Webview. Source navigation opens the currently validated source, not an old location promise. Captured historical pairs never become current Git/HEAD diffs and never overwrite source files. Complete/failed/interrupted tool status is separate from observed text differences; errors and Stop may follow partial side effects.

Capture uses approved write/edit's final authorization window for the before image and public tool completion for the after image, independent of bounded activity-display text. Concurrent/preflight-overlapping calls are disclosed, not attributed as isolated operations. The host watches the selected project during tasks for additional change metadata; without a before image it explicitly reports no captured diff instead of inventing one. Safe regular UTF-8 project files only: known sensitive paths/content, external targets, binary/oversized/unreliable reads get a reason, never a truncated or fabricated pair. Capture limitations do not silently approve/deny an otherwise authorized tool; dirty protection and its final recheck remain mandatory.

Technical retention bounds: 128 entries, 256 KiB UTF-8 per text snapshot, 8 MiB retained snapshot text per live runtime, 1 KiB UTF-8 display path, 16 rows per local UI page. No eviction of prior snapshots: entry/snapshot overflow is explicit, and later calls remain subject to normal approval. Reads have bounded size and deadline; late results cannot mutate replacement runtime state. Metadata updates do not reset the selected page or current draft. Snapshots survive view disposal/recreation but clear on runtime/resource/session replacement with an explicit reset notice. Opening a historical diff rechecks the current source and discloses later change/unavailability without changing the captured pair. Native opens are single-flight; repeated intents do not accumulate editor operations. Watcher work is serialized and coalesced by retained path with bounded pending work. Denied final authorization removes provisional captures without consuming retained entries/text. Reset/loss remains visible even when a disconnected runtime hides review actions.

### WI-017 T017-01/02 — Saved-session selection and sequential handoff (Build contract)

Goal-approved REQ-008 starts with public pi persistence, current-project listing, new conversations and deliberate restoration; complete progressive history/attachment recovery remains T017-03, not implied by a catalogue or probe. Agent execution stays in subprocess RPC. An adapter-owned, bounded short-lived helper may use the declared package's public SessionManager API for listing and project/identity checks; it does not read/write session-file formats itself. Returned storage paths are opaque host/adapter inputs only. Live conversations use pi's native persistence, not an extension-owned store; empty sessions may not appear until pi persists them.

New v2/viewId/generation intents: getSavedSessions(page: nonnegative safe integer), newConversation(no payload), resumeConversation(id: host-owned opaque catalogue ID). Native host confirmation precedes mutation; restoration requires confirming the other entry point is closed and discloses that confirmation is not an ownership lock. The confirmation also discloses Stop, unsent-draft/attachment loss and clearing grants/review snapshots. Cancel/stale confirmation performs no switch. A confirmed active task must settle through the existing Stop flow before replacement; failure does not start another session or silently retry a task. Resource consent and controlled defaults remain authoritative; historical extensions are not installed/loaded automatically. Project/selection identity is revalidated before starting the requested saved session.

The separate sessionState DTO projects phase (idle/listing/confirming/switching/error), current public session identity/name, loaded state, one 16-item catalogue page and total count, and fixed error codes. Catalogue rows expose only opaque UI ID, bounded title/excerpt and modified timestamp, never storage path or arbitrary SDK calls. Metadata bounds: title 160 characters, excerpt 256 characters, timestamp 40 characters. Only the currently issued page's IDs are actionable; stale selection requires refresh. The helper/host validate page requests and outcomes; catalogue refresh may reflect external changes and is not a locked historical listing.

Listing and switching have owned cancellation, deadlines and bounded output, not unbounded replay queues. Workspace/runtime replacement, disposal or stale native confirmation invalidates old work. A successful conversation replacement increments the Webview generation and clears temporary authorizations, pending model intents, review and attachment memory. Save/list identity is distinct from the adapter's numeric runtime generation. Failures are explicit and recoverable; not every public API success constitutes a complete restored-history UI.

### WI-017 T017-03 — Bounded restored history and retained text (Build contract)

The host restores the public SDK active branch, including available pre-compaction dialogue and generic tool/custom/summary entries, into a separate saved-history projection; it does not duplicate those rows in the live transcript. This presentation does not replace pi's model context. Initial display and each backwards/forwards page contain at most 32 chronological rows, with 4 KiB UTF-8 text per row and explicit truncation/unsupported-content notices. Historical tool names/status describe past records, not currently loaded capabilities or reconstructed success. Structured fallback has explicit depth/field/string limits and redacts credential-like field names; records explicitly marked `display: false` are omitted from both pages and preview ordinals. No historical renderer or extension code executes. Overview/preview projection consumes text segments incrementally rather than joining a complete large record.

The restored public branch's final entry is a host-only anchor. Later pages and previews are re-read through the public SDK and bounded to that anchor, so continuing a conversation cannot shift earlier-page offsets. A missing/noncurrent anchor fails explicitly instead of presenting a different branch. The extension does not add a session-file format, attachment store, global search or branch UI. The helper uses structured bounded requests, a 15-second deadline, 1 MiB response cap and observed child close; neither raw diagnostics nor storage paths reach the Webview.

New intents are getSavedHistory(page) and getSavedHistoryPreview(id, requestId, offset), under the normal v2/view/generation envelope. savedHistoryState carries availability, idle/loading/error, one bounded page, page/total and fixed errors. Host replaces SDK entry IDs with temporary row IDs; only the current window's IDs authorize retained-text previews. savedHistoryPreview correlates id/requestId and carries a fixed error or one literal text chunk of at most 8192 UTF-16 code units, offset/nextOffset/done and totalChars. Chunk boundaries preserve surrogate pairs. UI retains one render window and one preview chunk, discloses earlier content, and can navigate without accumulating the entire history.

The retained-text action presents available original public message text. Recognized explicit-context prompt payloads show their retained file/selection attachment text as a historical snapshot, with unavailable fields/content labeled; unknown formats remain literal with limitations. It never rereads the current source path as historical attachment content, reconstructs images, or guarantees extra persistence. Omitted structured/unsupported data remains disclosed even in the text preview.

Conversation/workspace replacement and disposal abort old reads and revoke row capabilities; view recreation cancels an in-flight preview but retains a completed immutable window. Catalogue/inspect work waits for already-admitted historical reads and actual prior helper ownership to settle; an obsolete native dialog is not part of that barrier. New historical reads competing with a catalogue receive a recoverable reply, not an indefinitely loading view. View disposal also cancels a pending catalogue. Host rechecks draft revision immediately before committing a confirmed switch, after asynchronous Stop/inspection. Only its own attachment-preparation cancellation revision can be adopted; later user edits still invalidate confirmation. A committed switch clears old attachment results as well as draft content. Once runtime replacement is committed, its completion survives view recreation without repeating a task; an old precommit view cannot authorize a new handoff. Pending model choices/grants/review/live attachment memory do not migrate. Budget performance, genuine-runtime, native-host and installed-package evidence are tracked in ACTIVE; this Build contract does not itself complete REQ-008 or accept a gate.

# 上游 Agent SDK / RPC 集成

[English](pi-integration.md) | 中文

- 翻译状态：Machine Draft
- 权威原文：[pi-integration.md](pi-integration.md)
- 原文版本：Uncommitted baseline
- 最近同步：2026-09-19

- 类型：指南
- 状态：Accepted
- 创建：2026-09-19
- 权威：通过 SDK/RPC 连接上游 coding-agent 运行时的模式

适用于本产品的 pi 运行时集成。通用规则来自 engineering-template SDK/RPC 技术包；产品已选行为与证据见下文。

## 何时加载与证据来源

涉及运行时启动、SDK/RPC 命令或事件、会话操作、资源加载、权限及上游依赖升级时加载。实现前阅读已批准架构／ADR、当前 WI、实际 gate ID、依赖固定版本及该已安装版本的公开文档。本地上游源码可解释行为，但不授权依赖私有模块或未文档化的文件布局。

记录准确包版本、公开入口、相关文档与观察行为。区分文档保证、spike 观察和假设。API 无法支持需求时记录缺口并重新讨论方案，不静默自行实现上游子系统。

通用 Promise 处理、取消、资源清理和 Node 进程规范遵循下方链接的 TypeScript/Node 指南，不在此重复。

## 集成职责

- 按产品决策流程选择内嵌 SDK 或子进程 RPC，不顺手切换进程模型。特权宿主拥有工作区策略、凭证和运行时生命周期；适配器将上游结果／事件映射到已文档化的产品契约；UI 仅接收批准的展示数据。
- 不重写上游 agent 循环、provider 栈或压缩机制。产品编排须使用公开 SDK/RPC API。
- 创建、恢复、切换等支持的操作使用上游会话 API。除非 Accepted ADR 明确授权例外，不为产品功能读写上游会话文件。返回的标识／路径作为公开 API 的不透明输入，不据此解析存储格式。
- 凭证和特权能力不得进入 UI 消息。不将低信任 UI 提交的任意 SDK 调用、命令名、shell 字符串或文件路径直接转发，须通过经校验、范围明确的宿主操作。

## RPC 与事件契约

- 按固定版本协议要求实现传输分帧，处理每次读取的半帧／多帧和文本编码分片；明确畸形、超大或截断输入的处理。协议输出与诊断分开，不假定一次 stdout 数据块就是一条消息。
- 读取字段前校验消息结构。区分命令响应、主动事件及协议支持的运行时向客户端请求。以待处理请求 ID 和预期操作关联响应；定义未知 ID、不支持消息和协议版本的处理。
- 分别定义命令确认、操作完成和流结束。保留所需事件顺序，不假定一次逻辑操作仅有一个事件，也不把重复出现的事件直接视为重复数据。
- 根据公开 API 证据明确允许的并发操作和取消语义。状态变更命令超时后，未确认结果与重试安全性前不重试。响应丢失不证明操作失败。
- 扩展适配器或 UI 桥前，在产品 reference 契约记录 DTO、错误和所有权。按实际成熟度使用 Planned/Outline/Living；写出 schema 不代表集成已实现。

## 运行时与会话生命周期

- 按需求定义可观察的启动中、就绪、停止中和失败状态；进程创建不等于就绪。启动失败或进程丢失时，结束待处理调用、使运行时身份失效，并通过宿主呈现失败。
- 停止、重启或切换会话／工作区时，明确处理待完成命令、流输出和审批请求。结果关联运行时／会话身份，防止旧回调影响替代会话。
- 验证上游会话切换、资源重载和取消的具体含义。未经文档与观察，不假定会话切换会更改 cwd、重载所有资源或重置权限。独立执行产品已批准的权限重置策略。
- 遵循 TypeScript 清理规则；集成检查还须观察子进程关闭与资源释放。发送终止信号不证明进程退出，短生命周期探针不等于生产会话管理器。

## 信任与资源加载

- 区分宿主工作区资格、上游项目资源批准和逐工具授权。工作区受信任不代表全部批准项目扩展或工具；资源加载选择不等于 shell／文件系统权限。
- 启动前核查文档默认值及持久／全局配置。产品要求明确选择资源时，传入受支持的显式控制并验证优先级，不依赖交互提示或默认拒绝的假设。
- 启动和切换时分别核查相关资源类别：扩展／包、设置、skills／prompts、上下文及全局资源。未观察类别明确报告，不由一个标记推断所有资源。
- 拒绝项目资源不一定阻止上下文读取、全局资源或普通工具访问。准确描述已验证范围。客户端或离线开关本身都不构成操作系统沙箱。

## Spike、回归检查与升级

- 从能回答 WI 问题的最小公开 API 探针开始。使用临时工作区／配置 fixture 和受控资源，隔离凭证、用户状态与环境。生命周期探针不得顺带安装项目包、执行真实项目扩展或调用模型。
- 测试相关成功与失败路径：启动失败、畸形协议、进程丢失、取消、迟到事件、切换和清理。断言禁止的动作未发生，例如不合格工作区不启动运行时。区分 mock 测试与真实运行时证据。
- 记录命令、准确版本／配置、预期与实际结果及剩余限制。未独立观察的资源或网络边界如实说明。有用发现写入现有讨论／WI 记录；满足所需验证与维护者确认后才写 Accepted ADR、关闭实际产品 gate。
- 升级时审阅公开 API／协议变化，隔离重跑受影响的生命周期、信任和会话检查。按已批准流程更新包固定版本／锁文件及相关决策记录。生产构建不得依赖未声明的同级仓库链接。

## 维护

集成交接时，将可复用协议或生命周期发现提炼到对应规则、链接证据并同步译文。版本特有事实与产品选择放在产品章节，与通用规则和历史观察分开。仅获授权时更新模板技术包。不因探针通过就将计划中的聊天、流式、会话管理或信任防护描述为已交付。

相关：[TypeScript/Node 指南](typescript.zh.md)。

## pi VS Code：已选方案与证据

[ADR 0001](../../decisions/0001-build-baseline.zh.md) 为基线和初始适配器方向选择子进程 RPC，固定 npm 包 `@earendil-works/pi-coding-agent@0.85.1`。现有探针以 `--mode rpc --no-session` 启动 `dist/bundle/cli.js`，通过 LF JSONL 发送 `get_state` 后停止。这是探针，不是长生命周期产品运行时。遵循[主架构](../../architecture/vscode-extension-architecture.zh.md)与实际 [gate 表](../../reference/architecture-gates.zh.md)，包括 `gate-runtime-host`；不为本项目编造 `gate-build-baseline`。

| 证据入口 | 支持的结论／限制 |
|----------------|--------------------------|
| [运行时探针](../../../src/adapter/pi-rpc-probe.ts)、[JSONL 辅助模块](../../../src/adapter/jsonl.ts)、`npm run spike:runtime`、[ADR 0001 的 WI-001 证据](../../decisions/0001-build-baseline.zh.md#spike-证据) | 一次请求／响应和关闭的历史证据，不证明流式、工具审批或生产并发。探针继承进程环境，不是隔离的信任验证程序。 |
| [信任验证程序](../../../scripts/spike-project-trust.mjs)、[fixture／生命周期辅助模块](../../../scripts/project-trust-lib.mjs)、[测试](../../../scripts/project-trust.test.mjs)、`npm run spike:project-trust` | 固定版本下隔离的启动与跨 cwd 会话切换场景。执行前阅读程序及其限制。 |
| [归档 WI-003 证据](../../archive/2026-09-21-closed-wi-history.zh.md#wi-003) | 已记录的六组信任场景观察，以及维护者仅对 spike 的验收。这是既有观察，不代表当前会话已重跑。 |
| [Webview 契约](../../reference/webview-messages.zh.md)、ACTIVE 的 WI-006 | 工作区资格与内存资源选择 UI，未启动运行时。选择已记录不等于资源已加载。 |
| [WI-007 归档](../../archive/2026-09-21-closed-wi-history.zh.md#wi-007)、[`pi-rpc-runtime`](../../../src/adapter/pi-rpc-runtime.ts) | 由宿主选择触发的长生命周期子进程 RPC；`get_state` 就绪；无聊天或流式。 |
| [WI-004 RPC 证据（0.85.1）](../../discussions/2026-09-21-wi-004-rpc-evidence-0.85.1.zh.md) | Prepare 阶段与固定版本一致的 `prompt`、`text_delta`、`turn_end` 与 `--no-tools` 文档；非 WI-004 实机验证。 |

### 项目信任发现与剩余限制

- 已记录的固定版本 spike 使用公开 `--approve` / `--no-approve` 覆盖参数，观察到显式拒绝覆盖全局 `always` 和已保存信任。不将裸 RPC 启动当作产品默认拒绝路径。后续运行时集成须消费宿主拥有的当前选择，复核资格／身份并应用受支持的显式控制。
- 已记录观察覆盖启动与切换时的项目扩展、skills、prompt templates 和 APPEND_SYSTEM 标记。拒绝路径仍存在全局扩展及 AGENTS.md 上下文。切换后旧项目命令消失；每次切换观察到两次 resume 事件，因此不能写死单事件假设。
- 未独立观察 settings 效果与 themes，刻意未安装项目包。系统提示观察来自公开会话 API，不是实际 provider 请求。扩大结论或关闭 gate 前须保留这些限制。
- 信任验证使用临时 cwd／home／配置、环境允许列表、公开 `PI_CODING_AGENT_DIR`、`PI_OFFLINE=1` 和 `PI_TELEMETRY=0`；会话 fixture 与已保存信任通过公开 API／hook 创建，不解析／写入会话或信任文件。这不提供 OS 网络沙箱或独立流量审计。ACTIVE 记录早期探索没有离线开关，不得追溯声称所有运行均隔离网络流量。
- 工作区资格、项目资源加载和工具审批保持独立。详细产品选择与重置规则属于 [PRD](../../product-requirements.zh.md) 和当前 WI，本指南不批准其实现。按 gate 表记录，`gate-project-trust`、`gate-webview-trust`、`gate-session-streaming` 仍为 Open。

文档变更使用 `npm run docs:verify` 及协作流程要求的 `npm run docs:health`；集成代码运行 compile、lint 和相关测试／spike，并遵循上述隔离要求。包升级须遵循 ADR 0001 的运行时 spike 与维护者复核要求，以及受影响的信任／会话回归检查。更新证据和限制，不改写历史结果。
